services:
  ebb:
    build: .
    image: ebb:latest
    container_name: ebb
    restart: unless-stopped
    volumes:
      - ./config.yml:/etc/ebb/config.yml:ro
    ports:
      - "18761:18761" # 查询 API；不对外提供查询可去掉
