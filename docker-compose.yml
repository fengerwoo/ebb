services:
  ebb:
    image: ghcr.io/fengerwoo/ebb:latest
    # 从源码构建时取消下一行注释，并用 docker compose up -d --build
    # build: .
    container_name: ebb
    restart: unless-stopped
    volumes:
      - ./config.yml:/etc/ebb/config.yml:ro
    ports:
      - "18761:18761" # 查询 API；不对外提供查询可去掉
